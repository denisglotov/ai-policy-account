{
  "name": "cifra-ruble-policy-account",
  "version": "1.0.0",
  "description": "ERC-4337 Smart Account wallet requiring AI-Oracle cryptographic ECDSA approval",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "lint": "solhint 'src/**/*.sol' 'script/**/*.sol'",
    "lint:all": "solhint 'src/**/*.sol' 'test/**/*.sol' 'script/**/*.sol'",
    "lint:fix": "solhint --fix 'src/**/*.sol' 'script/**/*.sol'",
    "format": "forge fmt",
    "format:check": "forge fmt --check"
  },
  "devDependencies": {
    "solhint": "^6.2.4"
  },
  "license": "MIT"
}
